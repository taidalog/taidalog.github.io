{
  "name": "taidalog.github.io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dotnet fable watch src --run npx vite",
    "build": "dotnet fable src --run npx vite build",
    "build:testing": "dotnet fable src --define TESTING --run npx vite build --mode testing --base /test.taidalog.github.io/"
  },
  "keywords": [],
  "author": "taidalog",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "vite": "^6.0.11"
  }
}
